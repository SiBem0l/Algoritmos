{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "SIEMPRE CON ESTE",
            "command": "powershell",
            "args": [
                "-Command",
                "gcc (Get-ChildItem -Filter *.c | ForEach-Object { $_.Name }) -g -o ${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}